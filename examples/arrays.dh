دالة بداية(): عدد {
    دع درجات: [عدد] = [72، 88، 95، 81]؛
    درجات[0] = درجات[0] + 5؛

    إذا (درجات[0] == 77 و درجات[1] == 88) {
        اطبع("array indexing works")؛
    } وإلا {
        اطبع("array indexing failed")؛
    }

    أعد درجات[0] + درجات[1] + درجات.طول؛
}
